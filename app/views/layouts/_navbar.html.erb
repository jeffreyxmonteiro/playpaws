<div class="navbar">
  <!-- Logo -->
  <a href="/" class="navbar-brand">
    <%= image_tag "logo.png", alt: "PlayPaws" %>
  </a>
  <!-- Right Navigation -->
  <div class="navbar-right hidden-xs hidden-sm">
    <!-- <a href="" class="navbar-item navbar-link">Messages</a> -->
    <!-- <a href="#" class="navbar-btn btn-medium">Browse</a> -->
    <!-- Profile picture with dropdown list -->
    <div class="navbar-item">
      <% if user_signed_in? || owner_signed_in? %>
        <div class="dropdown">
          <!-- <img src="https://images.pexels.com/photos/1239288/pexels-photo-1239288.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=750&w=1260" class="avatar dropdown-toggle" id="navbar-menu" data-toggle="dropdown"> -->
          <div class="avatar dropdown-toggle" id="navbar-menu" data-toggle="dropdown">
            <%#= image_tag(current_user.avatar_url) if user_signed_in? %>
            <%= image_tag(current_owner.avatar_url) if owner_signed_in? %>
          </div>
          <ul class="dropdown-menu dropdown-menu-right navbar-dropdown-menu">
            <!-- <li><a href="#">Profile</a></li> -->
            <li><%= link_to "Profile", profile_user_path %></li>
            <!-- <li><a href="#">Log Out</a></li> -->
            <li><%= link_to "Log Out", destroy_owner_session_path, method: :delete %></li>
          </ul>
        </div>
      <% else %>
      <%= link_to "Login", new_owner_session_path %>
      <% end %>
    </div>
  </div>
</div>
