Dog Create Page !!Still need to do embedding

<%= simple_form_for @dog do |f|%>
<%= f.input :name %>
<%= f.input :description %>
<%= f.input :breed %>
<%= f.input :images_url %>
<%= f.input :available_dates %>
<%= f.submit %>
<% end %>
